version: '3.8'

services:
  docling-serve:
    # Uses the locally built ROCm image
    image: docling-serve-rocm:latest
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
    security_opt:
      - seccomp:unconfined
